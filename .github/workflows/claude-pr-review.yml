name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  claude-review:
    uses: thepointatinfinity/.github/.github/workflows/claude-review-reusable.yml@main
    secrets: inherit
