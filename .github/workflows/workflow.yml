name: Merge and Review Gate

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  checks: read
  contents: read
  issues: read
  pull-requests: read
  statuses: read

jobs:
  merge-review-gate:
    uses: thepointatinfinity/.github/.github/workflows/merge-review-gate-reusable.yml@main
    secrets: inherit
